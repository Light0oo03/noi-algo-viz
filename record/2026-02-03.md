## 17:01
目标/需求：将后端接口在前端统一封装，集中管理调用。
变更摘要：新增 axios 客户端与 auth 接口封装，并替换 authStore 中的 fetch 调用。
影响文件：`front/src/api/client.ts`、`front/src/api/auth.ts`、`front/src/stores/authStore.ts`、`front/package.json`、`front/pnpm-lock.yaml`。
关键实现点：使用 `axios.create` 统一 baseURL 与 JSON 头；auth 模块提供 register/login/logout；在 store 内统一处理 axios 错误结构。
测试/验证：未运行（仅进行静态改动）。
风险/待办：无。

## 17:04
目标/需求：修复 CodeViewer/Home 与 tsconfig 的类型报错。
变更摘要：调整 tsconfig 继承路径与 include；为首页标签数据补充类型。
影响文件：`front/tsconfig.app.json`、`front/src/pages/Home.vue`。
关键实现点：显式指向 `@vue/tsconfig` 文件路径以消除找不到扩展配置；补充 `AlgoTab` 类型并用于 computed。
测试/验证：未运行（仅进行静态改动）。
风险/待办：无。

## 17:12
目标/需求：实现 DFS 算法可视化与代码展示。
变更摘要：新增 DFS 代码模板与 trace 生成；扩展 trace 类型与播放器以支持通用算法；界面加入 DFS 选择并显示栈。
影响文件：`front/src/core/algorithms/dfs.ts`、`front/src/core/algorithms/dfs-code.ts`、`front/src/core/trace/types.ts`、`front/src/core/trace/TracePlayer.ts`、`front/src/pages/AlgoPlayground.vue`、`front/src/components/StatePanel.vue`、`front/src/components/PlayerControls.vue`、`front/src/config/sideMenu.ts`。
关键实现点：使用显式栈与逆序入栈保证访问顺序；trace 步骤新增 DFS 类型与高亮行；状态面板根据算法显示队列/栈与提示。
测试/验证：未运行（建议在页面切换 DFS、播放/单步验证）。
风险/待办：无。

## 17:13
目标/需求：修复 DFS trace 的类型报错。
变更摘要：为邻居遍历增加空值保护，消除 TS 对数组越界的告警。
影响文件：`front/src/core/algorithms/dfs.ts`。
关键实现点：在读取邻居数组元素后先做 `undefined` 判断。
测试/验证：运行 `pnpm -C front build`（失败：仍需验证修复后）。
风险/待办：重新执行构建验证。

## 17:14
目标/需求：验证 DFS 改动构建通过。
变更摘要：无（仅执行构建）。
影响文件：无。
关键实现点：运行前端构建检查类型与打包流程。
测试/验证：`pnpm -C front build` 通过。
风险/待办：无。

## 17:19
目标/需求：切换 DFS 时避免播放仍使用 BFS 的 trace。
变更摘要：监听算法切换时清理播放器并重置可视化状态。
影响文件：`front/src/pages/AlgoPlayground.vue`。
关键实现点：切换算法时暂停播放、清空 trace、重置步骤与提示。
测试/验证：未运行（建议切换 BFS/DFS 后播放验证）。
风险/待办：无。

## 17:27
目标/需求：新增 Dijkstra 与 Prim/Kruskal 可视化算法。
变更摘要：增加三种算法的代码模板与 trace 生成，扩展图/trace 类型，界面接入算法选择与说明，更新 README。
影响文件：`front/src/core/algorithms/dijkstra.ts`、`front/src/core/algorithms/dijkstra-code.ts`、`front/src/core/algorithms/prim.ts`、`front/src/core/algorithms/prim-code.ts`、`front/src/core/algorithms/kruskal.ts`、`front/src/core/algorithms/kruskal-code.ts`、`front/src/core/graph/types.ts`、`front/src/core/trace/types.ts`、`front/src/pages/AlgoPlayground.vue`、`front/src/components/PlayerControls.vue`、`front/src/components/StatePanel.vue`、`front/src/config/sideMenu.ts`、`README.md`。
关键实现点：Dijkstra 使用最短路松弛并展示优先队列；Prim/Kruskal 采用边权最小选择并高亮树边；算法切换/文案统一更新。
测试/验证：未运行（建议 `pnpm -C front build` 验证）。
风险/待办：无。

## 17:29
目标/需求：修复新增算法的类型问题并完成构建验证。
变更摘要：补充空值保护与移除未使用类型引用；构建通过。
影响文件：`front/src/core/algorithms/dijkstra.ts`、`front/src/core/algorithms/prim.ts`。
关键实现点：队列取最小元素时增加空值保护，避免 noUncheckedIndexedAccess 报错。
测试/验证：`pnpm -C front build` 通过（有 chunk size 警告）。
风险/待办：无。

## 14:50
目标/需求：新增链表算法的独立可视化模块。
变更摘要：新增链表数据结构、Trace 播放器、可视化画布与控制器；实现反转/中点/判环/合并/删除倒数第 k 个的动画与代码高亮；新增链表可视化页面与入口；更新 README。
影响文件：`front/src/pages/LinkedListPlayground.vue`、`front/src/components/LinkedListCanvas.vue`、`front/src/components/LinkedListControls.vue`、`front/src/components/LinkedListStatePanel.vue`、`front/src/core/linked-list/*`、`front/src/router/index.ts`、`front/src/pages/Home.vue`、`front/src/i18n/home.ts`、`README.md`。
关键实现点：链表单独渲染与指针展示；算法 trace 同步高亮；支持 k 参数与多链表合并可视化。
测试/验证：`pnpm -C front build` 通过（有 chunk size 警告）。
风险/待办：无。

## 14:57
目标/需求：链表算法入口放回算法可视化页左侧目录。
变更摘要：左侧目录新增链表算法项；算法可视化页按算法类型切换图/链表渲染与控制器。
影响文件：`front/src/config/sideMenu.ts`、`front/src/pages/AlgoPlayground.vue`。
关键实现点：引入链表可视化组件与播放器；在 AlgoPlayground 中根据算法切换渲染与播放逻辑。
测试/验证：未运行（建议刷新页面切换链表算法验证）。
风险/待办：无。
