## 11:27
目标/需求：
修复树算法可视化（此前显示成图动画且无法正常播放），并让前端构建通过，完成后暂停等待验收。

变更摘要：
- 在 `AlgoPlayground.vue` 中正式接入树模块渲染链路：`TreeCanvas`、`TreeStatePanel`、`TreeControls`。
- 新增树模块状态与播放器联动：`treeVizState/treePlayerStatus/treeCurrentStep/treeTotalSteps/treePlayer`。
- 在播放控制主流程中增加树分支：`generateTrace/play/pause/step/stepBack/goToStep/reset`。
- 新增树输入能力：底部控制栏提供层序 CSV + `null` 输入（示例 `1,2,3,null,4`），包含格式校验与错误提示。
- 接入树算法代码展示与描述：前序/中序/后序/层序的代码与标题说明。
- 修复树 core 在 strict TS 下的报错（`TracePlayer` 语法、空值访问保护、初始状态构建索引安全）。
- 修复 `src/test-algos.ts` 构建阻塞（去除 `process` 依赖，按 `{ steps }` 结构校验 trace）。

影响文件：
- `front/src/pages/AlgoPlayground.vue`
- `front/src/core/tree/TracePlayer.ts`
- `front/src/core/tree/traversal.ts`
- `front/src/core/tree/level-order.ts`
- `front/src/core/tree/utils.ts`
- `front/src/test-algos.ts`

关键实现点：
- 树模块不再落入图模块分支，播放与状态独立管理。
- 统一沿用现有播放器交互模型（idle/ready/playing/paused/ended）。
- 输入非法时不触发 trace 生成，只给出可读提示，避免页面异常。
- `pnpm -C front build` 全量通过，确认类型与打包链路恢复正常。

测试/验证：
- 命令：`pnpm -C front build`
- 结果：通过（`vue-tsc -b` + `vite build` 成功）。

风险/待办：
- 仅完成树模块与构建修复，查找/排序模块仍待后续实现。
- 尚未进行浏览器人工交互回归（需你在页面验证树动画视觉与交互体验）。

## 11:38
目标/需求：
补齐树画布编辑交互能力，使其支持像图模块一样的拖拽、双击新增节点和两点连线。

变更摘要：
- 重写 `TreeCanvas.vue` 的交互层：
  - 支持节点拖拽移动（鼠标按下拖动）。
  - 支持画布双击新增节点（空白处双击）。
  - 支持两次点击连线（先选父节点，再选子节点）。
  - 连线遵循二叉树约束（一个父节点最多左右两个孩子、子节点只能有一个父节点、禁止成环）。
- 在树播放中禁用编辑并给出提示，避免播放状态与编辑状态冲突。
- 在 `AlgoPlayground.vue` 接入树编辑事件（`update:tree`/`note`），编辑后自动重置树播放器并保留新树用于后续播放。

影响文件：
- `front/src/components/TreeCanvas.vue`
- `front/src/pages/AlgoPlayground.vue`

关键实现点：
- `TreeCanvas` 内维护自定义坐标，新增/拖拽节点可立即可视化。
- 未连线节点也会渲染在画布下方区域，方便后续连接。
- 编辑完成后同步到 `treeModel`，树算法播放直接基于当前编辑结果。

测试/验证：
- 命令：`pnpm -C front build`
- 结果：通过。

风险/待办：
- 目前树编辑数据不会自动回写到输入框字符串（输入框仍可独立重建树）。

## 11:44
目标/需求：
修复树画布交互问题：拖拽不丝滑、新增节点编号重复、无法删除节点。

变更摘要：
- 树节点拖拽流畅性优化：去掉 `left/top` 位置的过渡动画，仅保留颜色与高亮过渡。
- 双击新增节点编号策略调整：
  - `id` 继续按最大 `id + 1`。
  - `value` 改为最大 `value + 1`，避免出现已有 7 后再次新增 7 的问题。
- 新增树节点删除能力（仿图模块）：
  - 右键节点弹出菜单，支持“删除节点”。
  - 删除时自动清理父节点对该节点的左右子引用。
  - 删除后自动重算根节点（优先无父节点的节点）。
- 画布交互完善：
  - 空白点击取消选择。
  - 画布右键关闭菜单。

影响文件：
- `front/src/components/TreeCanvas.vue`

关键实现点：
- 右键菜单状态（位置、目标节点）在 `TreeCanvas` 内维护。
- 删除逻辑保持树结构一致性，不会残留悬空子指针。
- 保持与现有 `AlgoPlayground` 的 `update:tree` 联动，编辑后可立即播放。

测试/验证：
- 命令：`pnpm -C front build`
- 结果：通过。

风险/待办：
- 当前树删除只支持节点，不支持单独删除某条边（如需可后续补充）。

## 11:49
目标/需求：
处理树模块交互细节：删除节点导致其他节点跳动、先点新节点再点旧节点连线提示不合理。

变更摘要：
- 连线逻辑优化为“自动判断方向”：点击两个节点后，先尝试 A->B，失败再尝试 B->A。
- 因此“先点新节点再点旧节点”现在可自动转成“旧节点作为父节点，新节点作为子节点”进行连线（若约束允许）。
- 稳定节点坐标：对所有出现过的节点缓存固定坐标，避免树结构变化时由自动布局重算引发跳位。

影响文件：
- `front/src/components/TreeCanvas.vue`

关键实现点：
- `onNodeClick` 增加双向 attach 尝试，失败才给出无法连线原因。
- `watch(nodes ids)` 中为新出现节点补齐 `customPositions`，后续渲染优先使用固定坐标。

测试/验证：
- 命令：`pnpm -C front build`
- 结果：通过。

风险/待办：
- 当前仍是“树形约束连线”（单父节点、最多两个孩子、禁止成环），若要完全图式自由连边需后续改数据结构。

## 09:50
目标/需求：
修复右键菜单交互体验：图模块右键菜单松开后消失；树模块点击空白后删除菜单不消失。

变更摘要：
- 图模块（GraphCanvas）：
  - 保留右键菜单机制，不改为右键立即删除。
  - 增加“右键打开菜单后的短时保护窗口”，避免右键触发后的同帧 click 立即清空菜单。
  - 右键菜单现在可通过单击右键稳定弹出，无需长按。
- 树模块（TreeCanvas）：
  - 给节点层容器增加空白点击处理。
  - 在树画布空白点击时统一清理选中态并关闭右键菜单。

影响文件：
- `front/src/components/GraphCanvas.vue`
- `front/src/components/TreeCanvas.vue`

关键实现点：
- Graph 使用 `contextMenuOpenedAt` 控制开菜单后的短时防抖，阻断误触发清空逻辑。
- Tree 在 `.tree-nodes` 层增加点击空白检测（`event.target === event.currentTarget`）来关闭菜单。

测试/验证：
- 命令：`pnpm -C front build`
- 结果：通过。

风险/待办：
- 若后续需要，Graph 菜单可再补 ESC 快捷关闭与触摸端长按适配。

## 09:59
目标/需求：
按反馈修复树/图交互：树增加恢复默认按钮；树连线“先点新再点旧”仍失败；图右键菜单需单击右键稳定弹出。

变更摘要：
- TreeControls 增加“恢复默认图”按钮，并在 AlgoPlayground 接入 `resetTreeToDefault`。
- Tree 连线策略再次优化：
  - 自动优先选择更符合直觉的父子方向（已有父节点的节点优先作为父节点）。
  - 支持重挂接：子节点已有父节点时会先解除旧父关系再挂接。
  - 父节点子位已满时会按位置偏好释放一个子位后挂接。
  - 完成挂接后自动修正根节点。
- 图模块保留右键菜单交互（非立即删除），通过菜单打开时机保护避免右键后菜单瞬间消失。

影响文件：
- `front/src/components/TreeControls.vue`
- `front/src/pages/AlgoPlayground.vue`
- `front/src/components/TreeCanvas.vue`
- `front/src/components/GraphCanvas.vue`

测试/验证：
- 命令：`pnpm -C front build`
- 结果：通过。

风险/待办：
- Tree 连线现在更“编辑友好”（允许重挂接），但这比严格二叉树构造规则更宽松。

## 16:31
目标/需求：
刷新页面后保持当前算法页面（例如树前序遍历），不回到默认 BFS。

变更摘要：
- 在 `AlgoPlayground` 中新增算法选择持久化：
  - 切换算法时写入 `localStorage`。
  - 页面初始化时读取并恢复上次算法。
  - 对存储值做合法性校验（仅允许当前菜单中启用的算法 key）。
- 联动优化：刷新恢复算法时同步展开对应侧边栏分组（如 tree/list/queue）。

影响文件：
- `front/src/pages/AlgoPlayground.vue`

关键实现点：
- 新增 `ALGO_STORAGE_KEY = noi-algo-viz:selected-algo`。
- 新增 `getInitialAlgo()` 与 `getSectionIdByAlgo()`。
- `selectedAlgo` 初始化改为读取持久化结果，不再硬编码 `bfs`。

测试/验证：
- 命令：`pnpm -C front build`
- 结果：通过。

风险/待办：
- 当前仅持久化算法类型，不持久化每个模块的临时输入（例如树输入框、链表参数等）。

## 16:58
目标/需求：
树编辑结果像图一样按账号持久化到后端，刷新后恢复对应账号自己的树。

变更摘要：
- 后端新增树存储接口：
  - `GET /tree`：获取当前登录用户的树。
  - `POST /tree`：保存当前登录用户的树。
- Prisma schema 新增 `UserTree` 表，并在 `User` 上增加 `tree` 关联。
- 前端 `AlgoPlayground` 接入树云端持久化：
  - 树编辑（画布编辑/输入框编辑/恢复默认）后调用 `/tree` 防抖保存。
  - 登录成功与鉴权恢复时自动加载 `/tree` 并恢复树状态。

影响文件：
- `backend/prisma/schema.prisma`
- `backend/src/modules/tree/tree.module.ts`
- `backend/src/modules/tree/tree.controller.ts`
- `backend/src/modules/tree/tree.service.ts`
- `backend/src/modules/app/app.module.ts`
- `front/src/pages/AlgoPlayground.vue`

关键实现点：
- 后端校验树结构后再入库，避免非法 JSON 污染。
- TreeService 使用 SQL upsert，实现“同账号覆盖保存、不同账号隔离”。
- 前端保存使用 300ms 防抖，避免高频请求。

测试/验证：
- `pnpm -C front build` 通过。

## 13:41
目标/需求：
按用户反馈修复交互：右键菜单无需长按；增加“有向/无向图”模式切换；补齐有向边持久化。

变更摘要：
- 图模式切换：
  - 新增控制栏 `radio`：`无向图 / 有向图`。
  - 无向图模式：仅创建无向边，不显示/不使用有向边输入。
  - 有向图模式：创建有向边，可手动输入 `u>v:w`。
- 右键菜单修复：
  - stage 点击关闭仅响应左键，右键不触发关闭。
  - 放宽“菜单刚打开防抖时间窗”，避免右键释放即关闭。
- 有向边持久化修复：
  - 本地新增账号隔离 key：`noi-algo-viz:graph-mode:*` 与 `noi-algo-viz:directed-edges:*`。
  - 图云端保存时，`/graph` payload 一并携带 `graphMode` 与 `directedEdgesInput`。
  - 登录加载图时恢复云端/本地图模式与有向边输入；退出时立即保存。
- 画布渲染修复：
  - 有向模式下隐藏无向边层，避免权值重复显示（例如 `2` 与 `1` 叠加）。

影响文件：
- `front/src/pages/AlgoPlayground.vue`
- `front/src/components/GraphCanvas.vue`
- `front/src/components/PlayerControls.vue`

测试/验证：
- `pnpm -C front build` 通过。

## 13:11
目标/需求：
继续下一步，把“有向边文本输入”升级为“画布可视化可编辑”。

变更摘要：
- `GraphCanvas.vue` 新增有向边覆盖层（箭头）：
  - 在 `directedMode` 下显示有向边箭头与权重标签。
  - 点击两个节点可直接创建有向边（起点->终点，默认权重 1）。
  - 支持选中与右键删除有向边。
  - 删除节点时会同时清理关联有向边。
- `PlayerControls.vue`：
  - 保持 `extra` 插槽，供图算法控制栏注入有向边输入框。
- `AlgoPlayground.vue`：
  - `GraphCanvas` 新增 `directed-mode` / `directed-edges` 入参。
  - 监听 `update:directed-edges`，回写为统一输入字符串并提示重新播放。

影响文件：
- `front/src/components/GraphCanvas.vue`
- `front/src/components/PlayerControls.vue`
- `front/src/pages/AlgoPlayground.vue`

测试/验证：
- `pnpm -C front build` 通过。

风险/待办：
- 当前画布创建有向边默认权重为 1；后续可补“有向边权重编辑”交互（双击边或右键编辑权重）。

## 13:26
目标/需求：
修复右键菜单体验与有向边刷新丢失问题。

变更摘要：
- 右键菜单交互修复：
  - `GraphCanvas` 外层关闭逻辑改为 `mousedown` 且仅左键关闭菜单，避免右键释放瞬间把菜单关掉导致“必须按住右键”。
  - 右键打开菜单后增加一次性抑制标记，避免同一事件链触发 stage click 立即清空菜单。
- 有向边显示冲突修复：
  - 在 `directedMode` 下隐藏无向边渲染层，只显示有向边箭头与其权重，避免 `0>1:2` 与无向边权重 `1` 叠加。
- 有向边持久化补齐（本地 + 云端）：
  - 新增账号隔离 localStorage key：`noi-algo-viz:directed-edges:<userId|guest>`。
  - `directedEdgesInput` 初始化、登录切换、退出登录均可恢复/保存。
  - 云端通过 `/graph` 一并保存（`graph` payload 增加 `directedEdgesInput` 字段），加载云端图时同步恢复。
  - 有向边输入变化时：本地立即保存；登录态下触发图云端防抖保存。

影响文件：
- `front/src/components/GraphCanvas.vue`
- `front/src/pages/AlgoPlayground.vue`

测试/验证：
- `pnpm -C front build` 通过。
- `pnpm -C backend exec tsc -p tsconfig.json` 通过。

风险/待办：
- 需在后端环境执行 `pnpm -C backend db:push`（或按现有 start 脚本自动 db push）以创建 `UserTree` 表后，云端树持久化才会生效。

## 17:05
目标/需求：
修复树持久化行为：所有本地修改都应持久化到 localStorage，并在登录账号下自动同步后端；切账号读取各自上次保存的树。

变更摘要：
- 前端树持久化改为“双写策略”：
  - 先写 localStorage（始终执行）。
  - 再防抖请求后端 `/tree`（有 token 时执行）。
- localStorage 改为按身份隔离 key：
  - 登录用户：`noi-algo-viz:tree:<userId>`。
  - 未登录访客：`noi-algo-viz:tree:guest`。
- 登录/鉴权恢复时：先恢复当前账号本地树，再拉取云端树覆盖并回写本地。
- 退出登录时：等待 logout 完成后恢复 guest 本地树，避免误用上个账号 key。
- 云端保存失败时显示提示：本地已保存，云端失败可稍后重试。

影响文件：
- `front/src/pages/AlgoPlayground.vue`

测试/验证：
- 命令：`pnpm -C front build`
- 结果：通过。

风险/待办：
- 若后端数据库未执行 `db:push` 创建 `UserTree`，云端保存会失败，但本地保存仍有效。

## 17:12
目标/需求：
调整树持久化策略：登录优先读取后端；结构修改本地+后端双持久化；退出登录时先保存后端；并将该机制写入项目规则。

变更摘要：
- Tree 持久化重构为：
  - 每次结构修改先写 localStorage（账号隔离 key），再防抖写后端。
  - 登录/鉴权恢复时优先读取后端树，后端无数据再回退本地。
  - 拉到后端树后回写本地缓存，保持一致。
  - 退出登录时先立即保存当前树到后端，再执行 logout。
- 新增 tree 本地存取工具：`getCurrentTreeStorageKey / saveTreeToLocal / loadTreeFromLocal / restoreTreeFromLocal`。
- 更新项目规则：
  - `.claude/CLAUDE.md` 增加“可视化结构修改必须本地+后端持久化且按账号隔离”规则。
  - `AGENTS.md` 增加相同持久化约束条目。

影响文件：
- `front/src/pages/AlgoPlayground.vue`
- `.claude/CLAUDE.md`
- `AGENTS.md`

测试/验证：
- `pnpm -C front build` 通过。
- `pnpm -C backend exec tsc -p tsconfig.json` 通过。

风险/待办：
- 若后端 `UserTree` 表未创建，云端写入会失败但本地仍可保存，需确保执行 `pnpm -C backend db:push`。

## 17:30
目标/需求：
修复树结构刷新后节点位置跳动问题。

变更摘要：
- 根因修复：树数据中新增并持久化节点坐标（`x/y`），避免刷新后重新按布局算法排布导致跳位。
- TreeCanvas 在关键更新点（新增、连线、拖拽结束、删除）都会快照当前所有节点坐标并随树结构一起保存。
- 前后端树结构校验同步支持 `x/y` 字段（可选且需为有限数值）。

影响文件：
- `front/src/core/tree/types.ts`
- `front/src/components/TreeCanvas.vue`
- `front/src/pages/AlgoPlayground.vue`
- `backend/src/modules/tree/tree.controller.ts`

关键实现点：
- 渲染优先使用节点自带 `x/y`，无坐标才回退自动布局。
- 拖拽结束时会把节点当前位置写回 tree model 并触发持久化链路。

测试/验证：
- `pnpm -C front build` 通过。
- `pnpm -C backend exec tsc -p tsconfig.json` 通过。

风险/待办：
- 旧数据（无 x/y）首次加载仍会按自动布局显示，保存一次后即固定坐标。

## 17:43
目标/需求：
继续完成“查找模块”可视化，实现可播放的算法动画并接入主页面。

变更摘要：
- 新增 `core/search`：
  - `types.ts`：查找可视化状态与 trace 类型。
  - `linear.ts`：线性查找 trace 生成。
  - `binary.ts`：二分查找 trace 生成（自动升序处理输入）。
  - `linear-code.ts` / `binary-code.ts`：代码展示与行号映射。
  - `TracePlayer.ts`：查找模块播放器。
- 新增组件：
  - `SearchCanvas.vue`：数组格子、状态高亮、指针（L/R/M/i）可视化。
  - `SearchStatePanel.vue`：目标值、指针状态、结果展示。
  - `SearchControls.vue`：算法切换、播放控制、进度条。
- `AlgoPlayground.vue` 接入查找模块：
  - 增加 `isSearchAlgo` 分支及画布/状态面板/控制器渲染。
  - 增加查找状态、输入（数组与 target）、播放器与 trace 生成流程。
  - 增加线性/二分代码与标题/描述映射。
  - 增加输入变更后的状态重置逻辑。
  - 快捷键撤销逻辑中排除查找模式。
- 侧边栏更新：`search` 分组从占位改为“线性查找/二分查找”。

影响文件：
- `front/src/config/sideMenu.ts`
- `front/src/pages/AlgoPlayground.vue`
- `front/src/core/search/types.ts`
- `front/src/core/search/linear.ts`
- `front/src/core/search/binary.ts`
- `front/src/core/search/linear-code.ts`
- `front/src/core/search/binary-code.ts`
- `front/src/core/search/TracePlayer.ts`
- `front/src/components/SearchCanvas.vue`
- `front/src/components/SearchStatePanel.vue`
- `front/src/components/SearchControls.vue`

测试/验证：
- `pnpm -C front build` 通过。

风险/待办：
- 本阶段仅完成“查找”模块；“排序”模块仍待后续实现。

## 18:12
目标/需求：
继续完成“排序模块”并接入主页面可播放链路。

变更摘要：
- 新增排序模式完整接线（`AlgoPlayground.vue`）：
  - 增加 `isSortAlgo` 分支，渲染 `SortCanvas/SortStatePanel/SortControls`。
  - 增加排序状态与播放器（`sortVizState`、`SortTracePlayer`）以及步骤统计。
  - 增加排序输入 `sortArrayInput` 与数组解析函数，支持输入变更后重置播放状态。
  - 在 `generateTrace/play/pause/step/stepBack/goToStep/reset` 中补齐排序分支。
  - 在算法代码/标题/名称/描述映射中接入 `bubble-sort`、`selection-sort`。
  - 图编辑禁用逻辑与键盘撤销逻辑中排除排序模式。
- 页脚增加排序输入框（逗号分隔整数），与排序控制器联动。

影响文件：
- `front/src/pages/AlgoPlayground.vue`

测试/验证：
- `pnpm -C front build` 通过。

风险/待办：
- 排序模块当前仅包含冒泡/选择；快速排序等后续算法可按同一接口继续扩展。

## 18:46
目标/需求：
将查找与排序扩展为“常见算法集合”，支持可视化播放与切换。

变更摘要：
- 查找模块从 2 种扩展为 5 种：
  - 已新增：跳跃查找、插值查找、指数查找。
  - 保留：线性查找、二分查找。
- 排序模块从 2 种扩展为 6 种：
  - 已新增：插入排序、快速排序、归并排序、堆排序。
  - 保留：冒泡排序、选择排序。
- 主页面接线更新：
  - `AlgoPlayground.vue` 新增上述算法的代码映射、标题/描述映射、`isSearchAlgo/isSortAlgo` 判定与 `generateTrace` 分发逻辑。
- UI 配置更新：
  - 侧边栏菜单项扩展（查找 5 项、排序 6 项）。
  - `SearchControls.vue`、`SortControls.vue` 的算法下拉选项同步扩展。

新增文件：
- `front/src/core/search/jump.ts`
- `front/src/core/search/interpolation.ts`
- `front/src/core/search/exponential.ts`
- `front/src/core/search/jump-code.ts`
- `front/src/core/search/interpolation-code.ts`
- `front/src/core/search/exponential-code.ts`
- `front/src/core/sort/insertion.ts`
- `front/src/core/sort/quick.ts`
- `front/src/core/sort/merge.ts`
- `front/src/core/sort/heap.ts`
- `front/src/core/sort/insertion-code.ts`
- `front/src/core/sort/quick-code.ts`
- `front/src/core/sort/merge-code.ts`
- `front/src/core/sort/heap-code.ts`

影响文件：
- `front/src/pages/AlgoPlayground.vue`
- `front/src/config/sideMenu.ts`
- `front/src/components/SearchControls.vue`
- `front/src/components/SortControls.vue`

测试/验证：
- `pnpm -C front build` 通过。

风险/待办：
- 新增算法步骤较多（尤其快排/归并/堆排），后续可按需增加“快速播放倍率”与“步骤过滤”优化体验。

## 19:05
目标/需求：
修复查找/排序数组刷新后回到默认值的问题，补齐本地持久化。

变更摘要：
- 在 `AlgoPlayground.vue` 为查找与排序新增账号隔离 localStorage：
  - 查找：保存 `arrayInput + target`。
  - 排序：保存 `arrayInput`。
  - key 规则：`noi-algo-viz:search:<userId|guest>`、`noi-algo-viz:sort:<userId|guest>`。
- 初始化时优先读取本地缓存作为输入默认值。
- 输入变更监听（`searchArrayInput/searchTarget/sortArrayInput`）中新增自动保存。
- 登录状态切换时自动恢复对应账号（或 guest）的查找/排序配置，避免账号间串数据。
- `AGENTS.md` 规则补充：search/sort 这类尚无后端模型的输入配置也必须至少做本地账号隔离持久化。

影响文件：
- `front/src/pages/AlgoPlayground.vue`
- `AGENTS.md`

测试/验证：
- `pnpm -C front build` 通过。

风险/待办：
- 若后续需要跨设备同步查找/排序输入，需新增后端模型与接口后升级为“本地 + 云端”双持久化。

## 19:42
目标/需求：
先产出“与对标站差距追踪文件”，并开始按清单编码。

变更摘要：
- 新增对标追踪文档：`record/totuma-gap-tracker.md`
  - 按 P0/P1/P2 列出差距、状态、验收标准与本周执行顺序。
- 启动并完成 P0 持久化项（查找/排序云端持久化）：
  - 后端新增 `/search`、`/sort` 模块与接口（GET/POST，账号隔离）。
  - Prisma 新增 `UserSearch`、`UserSort` 模型，`User` 关系字段同步扩展。
  - 前端 `AlgoPlayground.vue` 接入查找/排序云端加载与防抖保存：
    - 修改输入时：本地立即持久化 + 登录态自动同步后端。
    - 登录后：优先读取后端；无云端数据再回退本地。
    - 退出前：尝试立即同步查找/排序配置到后端。

影响文件：
- `record/totuma-gap-tracker.md`
- `backend/prisma/schema.prisma`
- `backend/src/modules/app/app.module.ts`
- `backend/src/modules/search/search.module.ts`
- `backend/src/modules/search/search.controller.ts`
- `backend/src/modules/search/search.service.ts`
- `backend/src/modules/sort/sort.module.ts`
- `backend/src/modules/sort/sort.controller.ts`
- `backend/src/modules/sort/sort.service.ts`
- `front/src/pages/AlgoPlayground.vue`

测试/验证：
- `pnpm -C front build` 通过。
- `pnpm -C backend exec tsc -p tsconfig.json` 通过。

风险/待办：
- 需要执行 `pnpm -C backend db:push` 创建/更新 `UserSearch`、`UserSort` 表后，云端持久化才能生效。

## 11:24
目标/需求：
修复登录时报 Prisma datasource 与 DATABASE_URL 不匹配的问题。

变更摘要：
- 修复 `backend/scripts/adjust-schema.js`：
  - 由“固定将 sqlite 改为 postgresql”改为“根据 `.env` 中 `DATABASE_URL` 自动选择 provider”。
  - 当 URL 为 `postgresql://` 或 `postgres://` 时设置为 `postgresql`，否则设置为 `sqlite`。
- 重新生成 Prisma Client，确保当前环境（sqlite）下运行时配置一致。

影响文件：
- `backend/scripts/adjust-schema.js`

测试/验证：
- `pnpm -C backend prisma validate` 通过。
- `pnpm -C backend prisma generate` 通过。
- `pnpm -C backend exec tsc -p tsconfig.json` 通过。

风险/待办：
- 切换数据库类型时需同步调整 `.env` 的 `DATABASE_URL`，脚本会自动切换 schema provider。

## 11:39
目标/需求：
按对标计划继续补齐图算法，优先实现 Floyd 与拓扑排序可视化。

变更摘要：
- 新增图算法：
  - `Floyd-Warshall`（全源最短路）trace 与代码高亮。
  - `拓扑排序（Kahn）` trace 与代码高亮。
- 菜单接入：
  - 图分组新增 `floyd`、`topological-sort` 两项。
- 主页面接入：
  - `AlgoPlayground.vue` 新增上述算法的导入、代码映射、标题/描述映射与 `generateTrace` 分发。
  - 对空图场景增加提示校验。
- 拓扑排序在当前“无向图编辑器”下采用演示规则：
  - 将每条无向边按节点编号定向为 `小编号 -> 大编号`，再执行 Kahn；避免改动编辑器交互即可演示核心流程。

影响文件：
- `front/src/core/algorithms/floyd.ts`
- `front/src/core/algorithms/floyd-code.ts`
- `front/src/core/algorithms/topological-sort.ts`
- `front/src/core/algorithms/topological-sort-code.ts`
- `front/src/config/sideMenu.ts`
- `front/src/pages/AlgoPlayground.vue`

测试/验证：
- `pnpm -C front build` 通过。
- `pnpm -C backend exec tsc -p tsconfig.json` 通过。

风险/待办：
- 后续若要“严格有向图拓扑排序”，需在 GraphCanvas 中增加有向边建模与箭头渲染。

## 11:52
目标/需求：
继续按 P0 计划补图算法，新增 Bellman-Ford 可视化。

变更摘要：
- 新增 Bellman-Ford 算法 trace 与代码高亮映射：
  - 支持轮次松弛、双向检查（适配当前无向边模型）与提前结束提示。
  - 起点固定为节点 `0`（与 BFS/DFS/Dijkstra 习惯一致）。
- 菜单接入：
  - 图分组新增 `bellman-ford`。
- 主页面接入：
  - `AlgoPlayground.vue` 增加 Bellman-Ford 的导入、代码/标题/描述映射与 `generateTrace` 分支。
  - 对“缺少起点 0”场景增加提示。

影响文件：
- `front/src/core/algorithms/bellman-ford.ts`
- `front/src/core/algorithms/bellman-ford-code.ts`
- `front/src/config/sideMenu.ts`
- `front/src/pages/AlgoPlayground.vue`

测试/验证：
- `pnpm -C front build` 通过。

风险/待办：
- 当前图编辑器仍是无向边模型；若后续要严格演示有向最短路，需要升级边方向建模与渲染。

## 12:08
目标/需求：
继续按规划扩展查找/排序常见算法。

变更摘要：
- 查找新增：
  - `Fibonacci 查找`（升序数组）。
- 排序新增：
  - `希尔排序`
  - `计数排序`（当前按非负整数处理）
  - `基数排序`（当前按非负整数处理）
- 接入范围：
  - 新增 core 算法实现 + 代码高亮映射文件。
  - 侧边栏菜单、查找/排序控制器下拉项更新。
  - `AlgoPlayground.vue` 中的算法识别、代码/标题/描述映射、`generateTrace` 分发全部接入。

影响文件：
- `front/src/core/search/fibonacci.ts`
- `front/src/core/search/fibonacci-code.ts`
- `front/src/core/sort/shell.ts`
- `front/src/core/sort/shell-code.ts`
- `front/src/core/sort/counting.ts`
- `front/src/core/sort/counting-code.ts`
- `front/src/core/sort/radix.ts`
- `front/src/core/sort/radix-code.ts`
- `front/src/config/sideMenu.ts`
- `front/src/components/SearchControls.vue`
- `front/src/components/SortControls.vue`
- `front/src/pages/AlgoPlayground.vue`

测试/验证：
- `pnpm -C front build` 通过。

风险/待办：
- 计数/基数排序目前将负数归零处理；若需要严格负数支持，后续可补“偏移量/符号位”版本。

## 12:23
目标/需求：
继续扩展查找常见算法，补哈希查找系列。

变更摘要：
- 新增查找算法：
  - `哈希查找（开放定址）`
  - `哈希查找（拉链法）`
- 新增代码映射文件与 trace 生成文件：
  - `front/src/core/search/hash-open-code.ts`
  - `front/src/core/search/hash-open.ts`
  - `front/src/core/search/hash-chain-code.ts`
  - `front/src/core/search/hash-chain.ts`
- 接入范围：
  - 侧边栏菜单新增两项哈希查找。
  - `SearchControls` 下拉新增两项哈希查找。
  - `AlgoPlayground.vue` 补齐识别分支、代码/标题/描述映射与 `generateTrace` 分发。

实现说明：
- 开放定址：基于线性探测构建哈希表并可视化探测过程，空槽显示为 `-1`。
- 拉链法：将同桶元素映射为连续显示区间并在桶内线性扫描，空桶显示为 `-1`。

影响文件：
- `front/src/core/search/hash-open-code.ts`
- `front/src/core/search/hash-open.ts`
- `front/src/core/search/hash-chain-code.ts`
- `front/src/core/search/hash-chain.ts`
- `front/src/config/sideMenu.ts`
- `front/src/components/SearchControls.vue`
- `front/src/pages/AlgoPlayground.vue`

测试/验证：
- `pnpm -C front build` 通过。

风险/待办：
- 当前哈希可视化为“教学化简表示”，未展示真实链表节点结构；若要更直观链式结构，可后续新增专用 HashTableCanvas。

## 12:36
目标/需求：
继续补图算法与查找算法，按对标范围扩展。

变更摘要：
- 图算法新增：
  - `关键路径（AOE）`（当前按小编号->大编号定向规则演示）。
  - 新增文件：
    - `front/src/core/algorithms/critical-path.ts`
    - `front/src/core/algorithms/critical-path-code.ts`
  - 侧边栏与 `AlgoPlayground.vue` 已接入代码映射、标题描述、trace 分发。
- 查找算法新增：
  - `哈希查找（开放定址）`
  - `哈希查找（拉链法）`
  - 新增文件：
    - `front/src/core/search/hash-open.ts`
    - `front/src/core/search/hash-open-code.ts`
    - `front/src/core/search/hash-chain.ts`
    - `front/src/core/search/hash-chain-code.ts`
  - 侧边栏、`SearchControls`、`AlgoPlayground.vue` 已接入完整播放链路。

影响文件：
- `front/src/core/algorithms/critical-path.ts`
- `front/src/core/algorithms/critical-path-code.ts`
- `front/src/core/search/hash-open.ts`
- `front/src/core/search/hash-open-code.ts`
- `front/src/core/search/hash-chain.ts`
- `front/src/core/search/hash-chain-code.ts`
- `front/src/config/sideMenu.ts`
- `front/src/components/SearchControls.vue`
- `front/src/pages/AlgoPlayground.vue`

测试/验证：
- `pnpm -C front build` 通过。

风险/待办：
- 关键路径当前依赖“无向边按编号定向”的演示规则；若要严格 AOE 网络，需要引入有向边编辑模式。

## 12:49
目标/需求：
继续推进“有向图语义”支持，先给拓扑排序/关键路径提供严格有向输入通道。

变更摘要：
- `PlayerControls.vue` 新增 `extra` 插槽，允许图算法页在控制栏插入额外输入。
- `AlgoPlayground.vue`：
  - 图算法控制栏新增“有向边输入框”（仅 `topological-sort` / `critical-path` 显示）。
  - 支持输入格式：`0>1:3,1>3:2`（权重 `w` 可省略，默认 `1`）。
  - 播放时优先解析并使用该有向边输入；为空时仍回退旧规则（小编号->大编号定向）。
  - 输入格式非法时给出明确提示并阻止播放。
- 算法函数升级：
  - `generateTopologicalSortTrace(graph, directedInput?)`
  - `generateCriticalPathTrace(graph, directedInput?)`
  - 兼容旧调用方式，不影响其他算法。

影响文件：
- `front/src/components/PlayerControls.vue`
- `front/src/pages/AlgoPlayground.vue`
- `front/src/core/algorithms/topological-sort.ts`
- `front/src/core/algorithms/critical-path.ts`

测试/验证：
- `pnpm -C front build` 通过。

风险/待办：
- 当前为“文本输入有向边”方案；后续如需更直观编辑体验，可继续做“画布直接创建有向边 + 箭头渲染”。

## 12:57
目标/需求：
修复“有向边输入后步骤无变化、非法格式未触发提示”的问题。

变更摘要：
- 根因修复：
  - 之前有向边输入变化后未清空旧 trace，播放器会继续复用历史步骤。
  - 有向边中引用不存在节点时被算法内部静默过滤，用户感知为“输入无效但无提示”。
- 修复方案：
  - 新增 `watch(directedEdgesInput)`：输入变化时重置图播放器到 idle，强制下次播放重新生成 trace。
  - 新增 `validateDirectedEdgesForGraph()`：校验输入边端点必须存在于当前图节点集合。
  - 在 `topological-sort` 与 `critical-path` 的 `generateTrace` 分支中：
    - 格式非法直接报错并阻止播放；
    - 节点不存在直接报错并阻止播放（会列出未知节点 id）。

影响文件：
- `front/src/pages/AlgoPlayground.vue`

测试/验证：
- `pnpm -C front build` 通过。
