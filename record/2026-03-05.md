## 17:12
目标/需求：
修复图模块在 `/playground` 中从“有向图”切换到“无向图”后，边仍显示箭头的问题。
变更摘要：
在图画布组件中为有向边渲染层增加模式判断，仅在 `directedMode` 为 `true` 时渲染 `v-arrow`，避免无向模式下残留有向箭头显示。
影响文件：
- front/src/components/GraphCanvas.vue
关键实现点：
- 有向边模板从无条件渲染改为条件渲染：`v-if="props.directedMode"`。
- 保持 `directedEdges` 数据不变，仅修正展示层逻辑，避免影响已有有向边编辑与切换回有向模式后的恢复体验。
测试/验证：
- 执行 `pnpm -C front build`，构建通过（`vue-tsc` 与 `vite build` 均成功）。
- 本地访问 `http://localhost:5173/playground` 进行页面检查，确认模式切换控件可用，问题修复后无向模式不再显示有向箭头。
风险/待办：
- 需继续关注图模块模式切换相关交互（创建/删除边、右键菜单、历史回退）在不同算法场景下是否一致。

## 17:59
目标/需求：
将图画布中的“操作”提示框和“说明/队列/节点状态”面板改为可拖动，并限制在画布范围内。
变更摘要：
为两个浮层新增拖拽手柄、拖拽状态和边界夹取逻辑；拖动时限制在各自容器内部，窗口尺寸变化时自动重新夹取位置。
影响文件：
- front/src/components/GraphCanvas.vue
- front/src/pages/AlgoPlayground.vue
关键实现点：
- `GraphCanvas`：给“操作”提示框增加 `hint-drag-handle`，支持鼠标拖拽；使用 `wrap` 容器尺寸和提示框尺寸做坐标 `clamp`，确保不出界。
- `AlgoPlayground`：给右下“说明面板”增加 `floating-panel-drag-handle`，支持鼠标拖拽；使用 `center` 区域做边界限制。
- 处理了 `el-main` 组件 `ref` 为组件实例的问题，拖拽边界计算改为基于 `$el` 的真实 DOM 尺寸。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
- 在 `http://localhost:5173/playground` 通过 Playwright 脚本拖拽两个面板到极限位置，结果：`floatingInsideCenter=true`、`hintInsideWrap=true`。
风险/待办：
- 当前拖拽位置为会话内状态，刷新后恢复默认位置；如需记忆位置，可后续做本地持久化（按账号隔离）。

## 18:05
目标/需求：
修复后端访问 `/sort` 时 Prisma 报错 `P2021`（`main.UserSort` 表不存在）。
变更摘要：
同步本地 SQLite 数据库到最新 Prisma schema，并调整开发启动脚本，确保后续 `dev` 启动前自动执行 `db push`。
影响文件：
- backend/package.json
- backend/prisma/prisma/dev.db（由 `prisma db push` 同步结构）
关键实现点：
- 执行 `pnpm -C backend db:push`，创建缺失的 `UserSort` 表并重新生成 Prisma Client。
- 将 `backend` 的 `dev` 脚本改为：`pnpm db:push && tsx watch src/main.ts`，避免新增模型后本地库未同步导致再次报错。
测试/验证：
- `pnpm -C backend db:push` 执行成功（数据库与 schema 同步）。
- `pnpm -C backend build` 执行成功（`prisma generate` + TypeScript 编译通过）。
风险/待办：
- 当前项目仍以 `db push` 为主，未建立 migration 历史；多人协作或生产环境建议补全 Prisma migration 流程。

## 18:10
目标/需求：
将图模块默认图改为 7 节点二叉树形态（0-1/2，1-3/4，2-5/6），并在画布中水平与垂直居中。
变更摘要：
更新默认图节点坐标与边关系，改为与示意图一致的树状结构；坐标按 `GraphCanvas` 900x640 画布居中布局。
影响文件：
- front/src/stores/graphStore.ts
关键实现点：
- 默认节点改为：0(450,130)、1(250,270)、2(650,270)、3(100,500)、4(390,500)、5(550,500)、6(800,500)。
- 默认边改为：0-1、0-2、1-3、1-4、2-5、2-6（权重均为 1）。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
- 使用“恢复默认图”会加载新的树状默认图布局。
风险/待办：
- 若本地或云端已有用户自定义图，进入页面会优先加载用户图；需点击“恢复默认图”查看新默认结构。

## 18:14
目标/需求：
将默认图布局调整得更紧凑。
变更摘要：
在保持中心对齐前提下，缩小默认图横向和纵向间距，使树形结构更集中。
影响文件：
- front/src/stores/graphStore.ts
关键实现点：
- 节点坐标调整为：0(450,170)、1(290,280)、2(610,280)、3(170,430)、4(360,430)、5(540,430)、6(730,430)。
- 边关系保持不变：0-1、0-2、1-3、1-4、2-5、2-6。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 如需更“贴近”或更“疏朗”，可继续微调第二层和叶子层的 x 间距。

## 18:20
目标/需求：
底部左侧算法选择框应与左侧菜单一致，能够选择所有算法，而不是仅显示当前模块的部分算法。
变更摘要：
将底部各控制组件的算法下拉从“组件内硬编码”改为“页面统一传入全量算法选项”，与侧边菜单保持同一数据源。
影响文件：
- front/src/pages/AlgoPlayground.vue
- front/src/components/PlayerControls.vue
- front/src/components/LinkedListControls.vue
- front/src/components/TreeControls.vue
- front/src/components/SearchControls.vue
- front/src/components/SortControls.vue
- front/src/pages/LinkedListPlayground.vue
关键实现点：
- 在 `AlgoPlayground` 中基于 `sideMenuSections` 扁平化生成 `allAlgoOptions`（过滤 disabled），并传给所有底部控件。
- 控件组件改为 `v-for` 渲染 `algoOptions`，不再写死选项，保证与左侧菜单一致。
- 补齐 `LinkedListPlayground` 对 `LinkedListControls` 的 `algoOptions` 传参，保持类型和编译通过。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 全量下拉项较长，后续可考虑分组或可搜索下拉（`filterable`）提升选择效率。
