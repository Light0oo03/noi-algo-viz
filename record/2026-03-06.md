## 17:14
目标/需求：
识别当前尚未完成功能并继续推进，实现排序模块缺失项“折半插入排序”。
变更摘要：
新增“折半插入排序”算法可视化能力，接入排序核心 trace、代码展示、菜单与播放逻辑，可在 `/playground` 与其他排序算法一致使用。
影响文件：
- front/src/core/sort/binary-insertion.ts
- front/src/core/sort/binary-insertion-code.ts
- front/src/config/sideMenu.ts
- front/src/pages/AlgoPlayground.vue
关键实现点：
- 新增 `generateBinaryInsertionSortTrace`：先二分定位插入点，再右移区间并插入 key，输出逐步可视化状态。
- 新增 `BINARY_INSERTION_SORT_CODE_JS` 与高亮行映射，支持代码面板联动。
- 在侧边栏排序菜单中增加“折半插入排序”。
- 在 `AlgoPlayground` 中完成导入、算法类型识别、标题/说明映射、播放 trace 分发。
测试/验证：
- 执行 `pnpm -C front build`，构建通过（`vue-tsc` 与 `vite build` 成功）。
风险/待办：
- 排序状态面板当前仅显示 `i/j/min` 三个指针；折半插入的 `left/right/mid` 已复用现有指针位展示，后续可考虑扩展更语义化指针显示。

## 17:38
目标/需求：
继续推进未完成项，补齐查找模块中的 `B 树查找` 可视化能力。
变更摘要：
新增 `B 树查找` 算法，接入侧边栏菜单、算法代码面板、查找 trace 生成和播放分发逻辑；在现有查找画布中复用数组视图展示 B 树键集合与查找路径。
影响文件：
- front/src/core/search/b-tree.ts
- front/src/core/search/b-tree-code.ts
- front/src/config/sideMenu.ts
- front/src/pages/AlgoPlayground.vue
关键实现点：
- 新增 B 树代码模板与高亮行映射 `B_TREE_SEARCH_CODE_JS/B_TREE_SEARCH_CODE_LINES`。
- 新增 `generateBTreeSearchTrace`：输入数组先去重排序，构建最小度 `t=2`（2-3-4 树）后执行查找，逐步记录“访问节点 / 比较 / 下沉 / 命中或失败”状态。
- 在 `AlgoPlayground` 中新增 `b-tree-search` 的算法分类、标题/名称/描述映射、代码展示与 trace 播放分支。
测试/验证：
- 执行 `pnpm -C front build`，构建通过（`vue-tsc` 与 `vite build` 均成功）。
风险/待办：
- 当前 B 树查找复用了线性数组画布来高亮键集合，尚未提供树形节点布局渲染；后续可增加专用 `BTreeCanvas` 提升教学表达。

## 17:47
目标/需求：
继续开发查找模块，补齐 `B+ 树查找`。
变更摘要：
新增 `B+ 树查找` 可视化算法，支持内部节点路由与叶子层扫描过程演示，并完成菜单、代码展示、播放分发接线。
影响文件：
- front/src/core/search/b-plus-tree.ts
- front/src/core/search/b-plus-tree-code.ts
- front/src/config/sideMenu.ts
- front/src/pages/AlgoPlayground.vue
关键实现点：
- 新增 `B_PLUS_TREE_SEARCH_CODE_JS/B_PLUS_TREE_SEARCH_CODE_LINES`。
- 新增 `generateBPlusTreeSearchTrace`：输入数组去重排序后构建静态 B+ 树分层结构，演示“内部节点按分隔键路由 -> 下降子节点 -> 叶子节点扫描”。
- 在 `AlgoPlayground` 中接入 `b-plus-tree-search` 的算法识别、标题与描述、代码显示、trace 生成分支。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 当前 B+ 树可视化仍复用数组画布表达键与路径，后续可增加专用树形布局和叶子链指针显示，增强教学直观性。
