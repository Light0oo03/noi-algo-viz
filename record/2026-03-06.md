## 17:14
目标/需求：
识别当前尚未完成功能并继续推进，实现排序模块缺失项“折半插入排序”。
变更摘要：
新增“折半插入排序”算法可视化能力，接入排序核心 trace、代码展示、菜单与播放逻辑，可在 `/playground` 与其他排序算法一致使用。
影响文件：
- front/src/core/sort/binary-insertion.ts
- front/src/core/sort/binary-insertion-code.ts
- front/src/config/sideMenu.ts
- front/src/pages/AlgoPlayground.vue
关键实现点：
- 新增 `generateBinaryInsertionSortTrace`：先二分定位插入点，再右移区间并插入 key，输出逐步可视化状态。
- 新增 `BINARY_INSERTION_SORT_CODE_JS` 与高亮行映射，支持代码面板联动。
- 在侧边栏排序菜单中增加“折半插入排序”。
- 在 `AlgoPlayground` 中完成导入、算法类型识别、标题/说明映射、播放 trace 分发。
测试/验证：
- 执行 `pnpm -C front build`，构建通过（`vue-tsc` 与 `vite build` 成功）。
风险/待办：
- 排序状态面板当前仅显示 `i/j/min` 三个指针；折半插入的 `left/right/mid` 已复用现有指针位展示，后续可考虑扩展更语义化指针显示。
