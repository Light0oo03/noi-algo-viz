## 20:18
目标/需求：
继续完善 B 树/B+ 树可视化，补充父子连线与 B+ 叶子链指针。
变更摘要：
在查找画布中为树形视图新增 SVG 连线层，自动绘制父子节点连接线；在 B+ 树模式下额外绘制叶子层链式箭头。
影响文件：
- front/src/components/SearchCanvas.vue
关键实现点：
- `SearchCanvas` 增加 `treeViewportRef` 与节点 DOM 引用映射，按节点布局实时计算连线坐标。
- 新增 `treeEdges`（父子连线）与 `leafEdges`（B+ 叶链）渲染，叶链使用虚线+箭头标记。
- 监听 `treeNodes/algoKey` 与窗口尺寸变化，自动重新计算连线位置，确保面板缩放后仍对齐。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 当前未绘制父子边权或分叉标签，后续可在教学模式中增加“选择子节点的比较依据”提示文本。

## 20:26
目标/需求：
继续提升 B 树/B+ 树教学可读性，在说明面板中直接展示“当前路由依据”。
变更摘要：
扩展查找状态字段 `routeHint`，并在 B 树/B+ 树 trace 中持续更新比较与下沉规则；状态面板新增“树路由”区块显示当前激活节点和路由规则。
影响文件：
- front/src/core/search/types.ts
- front/src/core/search/b-tree.ts
- front/src/core/search/b-plus-tree.ts
- front/src/components/SearchStatePanel.vue
关键实现点：
- `SearchVizState` 新增 `routeHint`，支持步骤级拷贝。
- B 树查找在“节点比较/下沉/叶子失败/命中”步骤中写入对应路由说明。
- B+ 树查找在“内部路由/叶子扫描/命中或失败”步骤中写入规则提示。
- `SearchStatePanel` 增加“🧭 树路由”区块，展示 `active-node` 和 `rule`。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 目前路由说明为纯文本，后续可结合连线动画高亮“本次选择的子边”。

## 20:34
目标/需求：
把“本次选择的子边”从文字提示升级为画布中的连线高亮。
变更摘要：
新增 `activeTreeEdge` 步骤状态字段；B 树与 B+ 树在“下降子节点”步骤写入当前父子边，`SearchCanvas` 根据该字段高亮对应连线。
影响文件：
- front/src/core/search/types.ts
- front/src/core/search/b-tree.ts
- front/src/core/search/b-plus-tree.ts
- front/src/components/SearchCanvas.vue
关键实现点：
- `SearchVizState` 新增 `activeTreeEdge` 并支持深拷贝。
- B 树/B+ 树 trace 在 `descend` 步骤设置 `{from,to}`，命中或失败时清空。
- `SearchCanvas` 为树连线增加 `active` 样式分支，实现当前选择子边的视觉高亮。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 当前仅高亮“最后一次下降边”，后续可叠加“已走过路径”渐进高亮。

## 20:40
目标/需求：
继续增强树路由可视化，增加“已走过路径”渐进高亮。
变更摘要：
新增 `visitedTreeEdges` 状态，B 树/B+ 树在每次下沉时累计路径边；`SearchCanvas` 为已访问边提供次级高亮样式，与当前高亮边区分展示。
影响文件：
- front/src/core/search/types.ts
- front/src/core/search/b-tree.ts
- front/src/core/search/b-plus-tree.ts
- front/src/components/SearchCanvas.vue
关键实现点：
- `SearchVizState` 新增 `visitedTreeEdges` 并支持深拷贝。
- trace 在 `descend` 步骤同时更新 `activeTreeEdge` 与 `visitedTreeEdges`。
- 画布新增 `visited` 边样式（浅蓝），`active` 边保持主高亮（深蓝）。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 当前“已走过路径”在重播时按步骤重建，后续可考虑增加“回退步骤时路径回滚动画”。

## 20:46
目标/需求：
继续提升路由教学表达，在当前高亮边上显示“本次选择的子节点索引”。
变更摘要：
新增 `activeChildIndex` 状态，并在 B 树/B+ 树下沉步骤写入；树连线中点显示 `child {index}` 标签，直观呈现当前路由决策。
影响文件：
- front/src/core/search/types.ts
- front/src/core/search/b-tree.ts
- front/src/core/search/b-plus-tree.ts
- front/src/components/SearchCanvas.vue
关键实现点：
- `SearchVizState` 新增 `activeChildIndex` 字段并支持深拷贝。
- trace 在 `descend` 步骤设置当前子节点索引；命中或失败后清空。
- `SearchCanvas` 依据 `activeTreeEdge + activeChildIndex` 计算中点标签并渲染。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 标签位置当前固定在连线中点，后续可根据连线斜率做偏移优化，减少与节点遮挡。

## 20:52
目标/需求：
继续优化树路由可视化，降低标签遮挡并补充图例说明。
变更摘要：
为树视图新增“连线图例”，并将 active 边标签改为按连线法向偏移，减少与节点/线段重叠；标签宽度改为按文本自适应。
影响文件：
- front/src/components/SearchCanvas.vue
关键实现点：
- 新增 `tree-legend`，展示当前边、已访问边、B+ 叶子链样式说明。
- `child index` 标签宽度自适应文本长度。
- 标签位置由“固定中点”改为“中点 + 法向偏移”，提升可读性。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 在极密集节点布局下仍可能出现局部重叠，后续可引入简单避让策略（比如与节点框碰撞检测后再偏移）。

## 20:57
目标/需求：
进一步减少 `child index` 标签遮挡，加入节点碰撞避让。
变更摘要：
将标签位置选择从“固定偏移”升级为“多候选点 + 碰撞评分”策略，优先选择不与节点框重叠且位移较小的位置。
影响文件：
- front/src/components/SearchCanvas.vue
关键实现点：
- 基于边中点生成多组法向候选位置（正向/反向/增距）。
- 对候选位置进行视口边界夹取，并与当前树节点矩形做碰撞检测计分。
- 选择“碰撞最少且位移最小”的候选点作为最终标签坐标。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 当前未引入节点外部留白权重，后续可结合路径曲率或局部拥挤度做更精细排版。

## 21:03
目标/需求：
继续增强路径表达，把“已访问节点”也可视化出来。
变更摘要：
新增 `visitedTreeNodeIds` 状态，B 树/B+ 树在访问和下沉时累计节点轨迹；画布对已访问节点使用次级高亮，并在图例中增加节点标识说明。
影响文件：
- front/src/core/search/types.ts
- front/src/core/search/b-tree.ts
- front/src/core/search/b-plus-tree.ts
- front/src/components/SearchCanvas.vue
关键实现点：
- `SearchVizState` 新增 `visitedTreeNodeIds` 并支持深拷贝。
- trace 在节点访问与下沉步骤持续更新访问节点集合。
- `SearchCanvas` 新增 `visited` 节点样式与图例项。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 叶子节点与已访问样式目前叠加较轻，后续可针对叶子访问态增加更明确视觉编码。

## 21:10
目标/需求：
继续增强树搜索可读性，在状态面板展示轨迹统计并优化叶子访问态视觉区分。
变更摘要：
`SearchStatePanel` 新增访问轨迹统计（节点数、边数、节点路径）；`SearchCanvas` 为“已访问叶子节点（非当前）”使用独立样式，区别于普通已访问内部节点。
影响文件：
- front/src/components/SearchStatePanel.vue
- front/src/components/SearchCanvas.vue
关键实现点：
- 面板新增 `visited-nodes`、`visited-edges` 与 `node-path`。
- `node-path` 使用等宽字体显示，便于观察路径序列。
- 叶子节点在 visited 态使用绿色系背景/边框，强化语义区分。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 路径文本较长时会换行，后续可增加折叠或复制按钮提升可用性。

## 22:18
目标/需求：
补齐树路由路径文本可用性，支持折叠展示与复制，并兼容受限剪贴板环境。
变更摘要：
`SearchStatePanel` 的 `node-path` 区域新增“展开/收起”“复制”按钮；长路径默认折叠两行；复制优先走 `navigator.clipboard`，失败后自动降级到 `document.execCommand('copy')`。
影响文件：
- front/src/components/SearchStatePanel.vue
- record/2026-03-09.md
关键实现点：
- 增加 `isPathExpanded`、`copyButtonText` 与路径变化重置逻辑。
- 添加 `copyText` 封装，统一处理现代复制 API 与兼容降级。
- 新增折叠样式 `-webkit-line-clamp: 2`，展开后显示完整路径文本。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 旧浏览器对 `execCommand` 的可用性依赖权限策略，若后续需要可补“手动复制弹窗”兜底。

## 22:27
目标/需求：
修复代码审核指出的复制降级清理缺陷，避免异常时遗留隐藏节点。
变更摘要：
将 `execCommand('copy')` 分支改为 `try/finally` 清理逻辑，确保复制成功、失败或抛错时都会移除临时 `textarea`。
影响文件：
- front/src/components/SearchStatePanel.vue
- record/2026-03-09.md
关键实现点：
- 引入 `textarea` 外部引用并在 `finally` 中按 `parentNode` 判定后执行移除。
- 保持原有返回语义：成功返回 `true/false`，异常返回 `false`。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 暂无新增风险。

## 22:46
目标/需求：
继续完善复制失败场景，为受限浏览器增加“手动复制”兜底交互。
变更摘要：
当自动复制失败时，在 `node-path` 区域展示手动复制面板，内含可选中文本框与关闭按钮；文本框聚焦时自动全选，减少手动操作成本。
影响文件：
- front/src/components/SearchStatePanel.vue
- record/2026-03-09.md
关键实现点：
- 新增 `showManualCopyFallback` 与 `manualCopyTextareaRef` 状态。
- 复制失败后展示兜底面板并自动选中文本。
- 路径变化时自动收起兜底面板，避免旧内容残留。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 暂无新增风险。

## 23:37
目标/需求：
提升手动复制兜底面板可用性，减少失败后的重复操作成本。
变更摘要：
在兜底面板新增“重试复制”按钮和平台快捷键提示（mac 显示 Command + C，其它显示 Ctrl + C）。
影响文件：
- front/src/components/SearchStatePanel.vue
- record/2026-03-09.md
关键实现点：
- 新增 `retryCopy` 复用复制逻辑，用户可在权限变化后再次尝试自动复制。
- 新增 `copyShortcutHint` 计算属性，按平台显示对应快捷键提示。
- 调整兜底面板头部操作区布局，支持“重试复制/关闭”双按钮。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 暂无新增风险。
