## 10:41
目标/需求：
修复“复制按钮在部分 B 树场景下不显示”的验收问题。
变更摘要：
将 `SearchStatePanel` 的“树路由”区块显示条件从仅依赖 `treeNodes` 改为“有路径或有树节点即显示”，确保 `node-path` 已存在时复制按钮可见。
影响文件：
- front/src/components/SearchStatePanel.vue
- record/2026-03-10.md
关键实现点：
- 新增 `showTreeRouteSection` 计算属性：`hasNodePath || treeNodes.length > 0`。
- 模板 `v-if` 改为使用 `showTreeRouteSection`。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 暂无新增风险。

## 11:02
目标/需求：
让“复制按钮”在 B 树/B+ 树说明面板中始终可见，避免用户无法定位按钮位置。
变更摘要：
`SearchStatePanel` 新增 `algoKey` 入参，并将“树路由”区块显示条件扩展为“树算法或存在路径/树节点”；同时复制/展开按钮在无路径时置灰而非隐藏。
影响文件：
- front/src/components/SearchStatePanel.vue
- front/src/pages/AlgoPlayground.vue
- record/2026-03-10.md
关键实现点：
- `SearchStatePanel` 增加 `isTreeSearchAlgo` 与新的 `showTreeRouteSection` 计算逻辑。
- `AlgoPlayground` 传递 `selectedAlgo` 到 `SearchStatePanel`。
- `path-btn` 支持 `:disabled=\"!hasNodePath\"`，并增加置灰样式。
测试/验证：
- 执行 `pnpm -C front build`，构建通过。
风险/待办：
- 暂无新增风险。

## 16:58
目标/需求：
修复 `/playground` 页面的 Element Plus `el-radio-button` 旧 API 告警，降低后续升级风险。
变更摘要：
将图模式切换按钮从 `label` 取值改为 `value` 取值，兼容 Element Plus 新版规范。
影响文件：
- front/src/pages/AlgoPlayground.vue
- record/2026-03-10.md
关键实现点：
- `el-radio-button label=\"undirected|directed\"` 调整为 `value=\"undirected|directed\"`。
- 保持 `graphMode` 现有逻辑与状态持久化行为不变。
测试/验证：
- 已执行：`pnpm -C front build`，构建通过。
- 已执行：打开 `/playground`，切换“无向图/有向图”正常，控制台未再出现该告警。
风险/待办：
- 无功能行为变化，属于 API 兼容性修复。

## 17:49
目标/需求：
优化“链表 / 反转链表”中间步骤可视化，避免第 4 步后画布只显示单节点导致理解困难。
变更摘要：
`LinkedListCanvas` 从“仅按 `head` 单链路渲染”改为“按根节点分段渲染”，中间态同时展示已反转段与待处理段，并在段尾明确显示 `null` 或“指向已有节点”。
影响文件：
- front/src/components/LinkedListCanvas.vue
- record/2026-03-10.md
关键实现点：
- 新增 `listSegments` / `buildRoots` / `buildSegment` 逻辑，优先使用反转链表关键指针（`prev/curr/next`）组织显示顺序。
- 模板改为按 `list-segment` 渲染节点和箭头，段尾根据 `tailType` 显示 `null` 或 `↩ 指向 #id`。
- 新增 `link-tag` 样式区分“回指已有节点”和“环形回指”。
测试/验证：
- 已执行：`pnpm -C front build`，构建通过。
风险/待办：
- 仅变更链表画布渲染策略，未改动链表 trace 生成逻辑；如需进一步增强，可在后续增加“已反转段/待处理段”文字标签。
