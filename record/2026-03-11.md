## 00:49
目标/需求：
建立“本地双 Agent（开发/评审）协作闭环”，替代受配额影响的 GitHub `@codex review` 强依赖流程。
变更摘要：
更新仓库协作规范，新增本地评审单模板 `record/review/current.md`，统一评审意见写入与状态流转规则。
影响文件：
- AGENTS.md
- record/review/current.md
- record/2026-03-11.md
关键实现点：
- `AGENTS.md` 新增 `Dual-Agent Local Review Workflow (must)`，定义角色分工、状态门禁与问题记录格式。
- 移除“每次 push 必须触发 GitHub Codex 审核”的强制规则，改为可选补充流程。
- 新建评审模板，标准化 `OPEN/PASS`、`P0/P1/P2`、`文件:行` 等审查字段。
测试/验证：
- 已执行：检查 `AGENTS.md` 规则条目与 `record/review/current.md` 模板内容完整。
风险/待办：
- 需要评审 Agent 与开发 Agent 严格遵循同一评审单，避免并发覆盖。

## 00:57
目标/需求：
进一步实现双 Agent 无人工转述协作，增加仓库内通信总线文件。
变更摘要：
新增 `record/review/bus.md` 作为开发 Agent 与评审 Agent 的状态通信通道，并在 `AGENTS.md` 增加 `Agent Bus Protocol` 强制规则。
影响文件：
- AGENTS.md
- record/review/bus.md
- record/2026-03-11.md
关键实现点：
- 定义状态机：`DEV_READY` / `REVIEW_OPEN` / `REVIEW_PASS`。
- 约束开发与评审双方的状态更新时机、提交哈希回填与冲突处理策略。
- 初始化 `bus.md` 模板与最近事件记录，便于两端 agent 直接轮询文件通信。
测试/验证：
- 已执行：检查 `AGENTS.md` 新增协议段落与 `bus.md` 状态说明、字段完整性。
风险/待办：
- 需双方 agent 均遵守“仅通过 `bus.md` 传递状态”的约定，避免多通道造成状态分叉。

## 01:10
目标/需求：
继续推进 `P1 交互能力`，为递归排序算法补充“递归栈视图”。
变更摘要：
在排序可视化状态中新增 `callStack` 快照，并让快速排序、归并排序在 trace 中记录递归进入/左右分支/合并/返回阶段；说明面板新增“递归栈”区块展示当前调用链。
影响文件：
- front/src/core/sort/types.ts
- front/src/core/sort/quick.ts
- front/src/core/sort/merge.ts
- front/src/components/SortStatePanel.vue
- record/2026-03-11.md
关键实现点：
- `SortVizState` 新增 `callStack` 与 `SortCallFrame`，在克隆状态时同步深拷贝。
- `generateQuickSortTrace` 与 `generateMergeSortTrace` 记录 `enter/left/right/merge/base/done` 阶段。
- `SortStatePanel` 渲染递归栈列表，并高亮当前栈顶调用帧。
测试/验证：
- 已执行：`pnpm -C front build`，构建通过。
风险/待办：
- 当前递归栈仅接入快速排序与归并排序；后续若增加更多递归算法，可沿用同一状态结构扩展。
