## 00:49
目标/需求：
建立“本地双 Agent（开发/评审）协作闭环”，替代受配额影响的 GitHub `@codex review` 强依赖流程。
变更摘要：
更新仓库协作规范，新增本地评审单模板 `record/review/current.md`，统一评审意见写入与状态流转规则。
影响文件：
- AGENTS.md
- record/review/current.md
- record/2026-03-11.md
关键实现点：
- `AGENTS.md` 新增 `Dual-Agent Local Review Workflow (must)`，定义角色分工、状态门禁与问题记录格式。
- 移除“每次 push 必须触发 GitHub Codex 审核”的强制规则，改为可选补充流程。
- 新建评审模板，标准化 `OPEN/PASS`、`P0/P1/P2`、`文件:行` 等审查字段。
测试/验证：
- 已执行：检查 `AGENTS.md` 规则条目与 `record/review/current.md` 模板内容完整。
风险/待办：
- 需要评审 Agent 与开发 Agent 严格遵循同一评审单，避免并发覆盖。

## 00:57
目标/需求：
进一步实现双 Agent 无人工转述协作，增加仓库内通信总线文件。
变更摘要：
新增 `record/review/bus.md` 作为开发 Agent 与评审 Agent 的状态通信通道，并在 `AGENTS.md` 增加 `Agent Bus Protocol` 强制规则。
影响文件：
- AGENTS.md
- record/review/bus.md
- record/2026-03-11.md
关键实现点：
- 定义状态机：`DEV_READY` / `REVIEW_OPEN` / `REVIEW_PASS`。
- 约束开发与评审双方的状态更新时机、提交哈希回填与冲突处理策略。
- 初始化 `bus.md` 模板与最近事件记录，便于两端 agent 直接轮询文件通信。
测试/验证：
- 已执行：检查 `AGENTS.md` 新增协议段落与 `bus.md` 状态说明、字段完整性。
风险/待办：
- 需双方 agent 均遵守“仅通过 `bus.md` 传递状态”的约定，避免多通道造成状态分叉。

## 01:10
目标/需求：
继续推进 `P1 交互能力`，为递归排序算法补充“递归栈视图”。
变更摘要：
在排序可视化状态中新增 `callStack` 快照，并让快速排序、归并排序在 trace 中记录递归进入/左右分支/合并/返回阶段；说明面板新增“递归栈”区块展示当前调用链。
影响文件：
- front/src/core/sort/types.ts
- front/src/core/sort/quick.ts
- front/src/core/sort/merge.ts
- front/src/components/SortStatePanel.vue
- record/2026-03-11.md
关键实现点：
- `SortVizState` 新增 `callStack` 与 `SortCallFrame`，在克隆状态时同步深拷贝。
- `generateQuickSortTrace` 与 `generateMergeSortTrace` 记录 `enter/left/right/merge/base/done` 阶段。
- `SortStatePanel` 渲染递归栈列表，并高亮当前栈顶调用帧。
测试/验证：
- 已执行：`pnpm -C front build`，构建通过。
风险/待办：
- 当前递归栈仅接入快速排序与归并排序；后续若增加更多递归算法，可沿用同一状态结构扩展。

## 01:13
目标/需求：
修复递归栈视图首次接线遗漏，确保排序播放状态能同步显示 `callStack`。
变更摘要：
在 `AlgoPlayground` 的 `syncSortVizState` 中补齐 `callStack` 同步，解决右下说明面板始终不显示“递归栈”区块的问题。
影响文件：
- front/src/pages/AlgoPlayground.vue
- record/2026-03-11.md
关键实现点：
- `syncSortVizState` 新增 `sortVizState.callStack = state.callStack`。
- 保持排序模块其余 `items/itemStates/pointers/note/highlightLines` 现有同步逻辑不变。
测试/验证：
- 已执行：`pnpm -C front build`，构建通过。
风险/待办：
- 暂无新增风险，待 AgentB 复审确认。

## 01:28
目标/需求：
响应验收反馈，避免将递归栈塞入说明面板，改为直接在排序画布中可视化展示，并将该交互原则固化为仓库规则。
变更摘要：
`SortCanvas` 新增右侧画布内递归栈区块，直接展示调用链；`SortStatePanel` 移除递归栈内容；`AGENTS.md` 新增“Visualization-First Rule”，约束说明面板仅保留轻量信息。
影响文件：
- front/src/components/SortCanvas.vue
- front/src/components/SortStatePanel.vue
- AGENTS.md
- record/2026-03-11.md
关键实现点：
- 排序画布调整为双区块布局，在有 `callStack` 时于画布内固定展示递归栈。
- 递归栈支持高亮当前栈顶调用帧，并在移动端自动改为上下布局。
- 新增仓库规则：可图形化的时序/层级信息优先在画布中表达，不堆到说明面板。
测试/验证：
- 已执行：`pnpm -C front build`，构建通过。
风险/待办：
- 当前“画布内递归栈”先接入排序模块；后续搜索路径、调用链等类似信息可按同一原则逐步迁移。

## 01:37
目标/需求：
继续优化排序画布布局与信息遮挡问题，解决负索引指针越界、递归栈展示方向不直观，以及说明面板遮挡主视图的问题。
变更摘要：
排序画布改为“上方横向递归栈 + 下方居中柱状图”布局，并给页面增加说明面板显隐按钮；同时在仓库规则中补充“说明面板遮挡主视图时必须迁移到画布”的约束。
影响文件：
- front/src/components/SortCanvas.vue
- front/src/pages/AlgoPlayground.vue
- AGENTS.md
- record/2026-03-11.md
关键实现点：
- 柱状图区域改为居中靠下布局，指针层增加前置偏移，保证 `i = -1` 仍在画布范围内。
- 递归栈视图迁移为顶部横向卡片流，更适合表达调用链顺序。
- `AlgoPlayground` 新增说明面板显隐按钮，使用 `View/Hide` 图标控制右下说明面板显示状态。
测试/验证：
- 已执行：`pnpm -C front build`，构建通过。
风险/待办：
- 当前说明面板显隐为页面内状态，刷新后会恢复默认显示；若后续需要，可再补本地持久化。

## 01:42
目标/需求：
将排序递归栈从“画布中文字列表”升级为真正的动画可视化，强化区间与调用流向表达。
变更摘要：
递归栈卡片新增区间覆盖条、阶段颜色、流向箭头，并通过 `TransitionGroup` 增加入栈/出栈动画，避免仅靠文字阅读调用过程。
影响文件：
- front/src/components/SortCanvas.vue
- record/2026-03-11.md
关键实现点：
- 每个调用帧根据 `[left, right]` 映射成覆盖数组长度的区间条。
- 不同阶段使用不同色带，区分递归进入、左右分支、基线返回与合并/完成。
- 使用 `TransitionGroup` 为调用帧增删和位移提供过渡动画。
测试/验证：
- 已执行：`pnpm -C front build`，构建通过。
风险/待办：
- 当前动画主要依赖步骤切换驱动，后续如需更强的时间轴感，可继续加入帧间连线变形或路径高亮。

## 02:03
目标/需求：
继续推进 `P0 持久化一致性`，增强查找/排序后端持久化的稳定性，避免数据库缺表时接口直接报错。
变更摘要：
将 `SearchService` / `SortService` 改为“读写前确保表存在”的自愈式实现，并统一改用 SQLite 原生 SQL 读写，补齐与 `TreeService` 一致的容错模式。
影响文件：
- backend/src/modules/search/search.service.ts
- backend/src/modules/sort/sort.service.ts
- record/2026-03-11.md
关键实现点：
- 新增 `ensureTable()`，在读写前执行 `CREATE TABLE IF NOT EXISTS`。
- `getSearch/getSort` 改为原生 SQL 查询首行并解析 JSON。
- `saveSearch/saveSort` 改为 `INSERT ... ON CONFLICT DO UPDATE`，避免依赖缺表时的 Prisma model API 直接抛错。
测试/验证：
- 已执行：`pnpm -C backend build`，构建通过。
风险/待办：
- 仍建议通过一次真实登录与刷新流程做联调验收，确认前端按账号恢复查找/排序配置。

## 15:11
目标/需求：
继续补齐 `P0 持久化一致性` 的前端闭环，解决“游客先编辑查找/排序，再登录账号时配置无法迁移并补传云端”的问题。
变更摘要：
`AlgoPlayground` 新增“优先账号本地、回退 guest 本地、并在云端缺数据时自动补传”的恢复策略，确保查找/排序配置在首次登录场景下也能平滑继承与落库。
影响文件：
- front/src/pages/AlgoPlayground.vue
- record/2026-03-11.md
关键实现点：
- 新增按指定 `userId` 读写本地查找/排序配置的方法。
- 新增 `restoreSearchFromBestLocal` / `restoreSortFromBestLocal`，在登录后优先取账号本地，没有则回退 guest 本地。
- 云端无数据时，恢复本地配置后立即调用 `saveSearchImmediately` / `saveSortImmediately` 反向补传到账号云端。
测试/验证：
- 已执行：`pnpm -C front build`，构建通过。
- 已执行：`pnpm -C backend build`，构建通过。
风险/待办：
- 建议做一次真实“guest -> 登录 -> 刷新”手工链路验证，确认恢复提示文案与云端补传时机符合预期。
