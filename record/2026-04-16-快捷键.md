## 11:02
目标/需求：
给动画播放增加键盘快捷键，支持 `Cmd/Ctrl + 左方向键` 上一步、`Cmd/Ctrl + 右方向键` 下一步。

变更摘要：
在 `/playground` 统一键盘事件处理中接入播放快捷键，使图、链表、栈、队列、树、查找、排序模块都可通过组合键进行单步控制。

影响文件：
- front/src/pages/AlgoPlayground.vue
- record/2026-04-16-快捷键.md

关键实现点：
- 复用现有 `step()` 与 `stepBack()`，不新增重复播放逻辑。
- 根据当前模块动态读取对应 player 状态，播放中不响应快捷键，避免和自动播放冲突。
- 保留原有图编辑 `Cmd/Ctrl + Z/Y` 撤销重做逻辑，仅在方向键组合下优先走播放控制。

测试/验证：
- 待执行：`pnpm -C front build`

风险/待办：
- 组合键依赖浏览器默认行为拦截，后续可再补帮助提示，明确快捷键可用范围。
