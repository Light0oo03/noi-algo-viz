## 10:34
目标/需求：
修复排序模块中 `i/j/min` 指针与数组下标不对齐的问题，避免 `0` 被显示到第 1 个元素下方。

变更摘要：
调整 `SortCanvas` 中排序指针的水平定位公式，去掉多余的一格偏移，使指针位置与数组下标一致。

影响文件：
- front/src/components/SortCanvas.vue
- record/2026-04-16.md

关键实现点：
- 将 `pointerStyle(index)` 中的定位从 `(index + 1) * 62 + 26` 改为 `index * 62 + 26`。
- 保持条形图间距、容器内边距和指针样式不变，仅修正下标到画布坐标的映射。

测试/验证：
- 待执行：`pnpm -C front build`

风险/待办：
- 需继续留意少数排序算法里指针名是否始终表示数组下标，避免后续出现“语义正确但标签名易误解”的问题。
